module AlertsHelper
    # def get_state(latitude, longitude)
    #     geo_localization = "#{latitude},#{longitude}"
    #     query = Geocoder.search(geo_localization).first
    #     query
    # end
    
    # def get_zone(result)
    #     result.data["address_components"].each do |component|
    #       if component["types"].include?("sublocality")
    #         zone = component["short_name"]
    #         return zone
    #       end
    #     end
    # end 
end
